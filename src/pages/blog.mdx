import ImageWrapper from "../components/image-wrapper"
import { graphql } from "gatsby"
import Img from "gatsby-image"
import SEO from "../components/seo"

<SEO title="Blog" keywords={["shlomi cohen", "application", "blog"]} />

export const pageQuery = graphql`
  {
    soon: file(relativePath: { eq: "images/soon.png" }) {
      childImageSharp {
        fluid(maxWidth: 500) {
          ...GatsbyImageSharpFluid_noBase64
        }
      }
    }
  }
`

<ImageWrapper>
  <Img fluid={props.data.soon.childImageSharp.fluid} />
</ImageWrapper>

## soon...
